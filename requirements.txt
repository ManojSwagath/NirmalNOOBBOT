# ============================================================
# Empathetic AI Companion — Python Dependencies
# ============================================================
# Install with:   pip install -r requirements.txt
#
# Raspberry Pi 5 extra steps (run BEFORE pip install):
#   sudo apt update
#   sudo apt install espeak-ng libespeak1 libatlas-base-dev libhdf5-dev libgtk-3-dev
# ============================================================

# --- Computer Vision ---
opencv-python>=4.8.0            # Webcam capture + face overlay display

# --- Emotion Detection (ONNX Runtime, lightweight ~50 MB) ---
onnxruntime>=1.15.0             # Runs emotion-ferplus CNN without TensorFlow

# --- Text-to-Speech (offline, no API key needed) ---
pyttsx3>=2.98                   # Cross-platform TTS (SAPI5 on Windows, espeak on Linux)

# --- Speech Recognition ---
SpeechRecognition>=3.10         # Microphone input → text (uses Google free API)
PyAudio>=0.2.14                 # Microphone access for SpeechRecognition

# --- Groq AI API (fast LLM for empathetic conversation) ---
groq>=0.9.0                     # Groq Python client
python-dotenv>=1.0.0            # Loads GROQ_API_KEY from .env file automatically

# --- Numerical (auto-installed by opencv/tensorflow, pinned for clarity) ---
numpy>=1.24
